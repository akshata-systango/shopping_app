import React,{useState} from "react";

const ProductSorting = () => {
   const [sortingchoice,setSortingChoice] = useState(['Ascending','Descending']);
   if(sortingchoice === 'Ascending'){
       setSortingChoice()
   }

  return <>
  </>;
};

export default ProductSorting;
